diff --git a/Cargo.toml b/Cargo.toml
index 6521c66..2465d8f 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -11,7 +11,7 @@ edition = "2018"
 structopt = "0.3"
 libc = "0.2"
 tokio = { version = "0.2", features = ["signal", "time", "rt-core", "macros", "net", "stream", "sync", "io-util"] }
-redbpf = { version = "1.2", features = ["load"] }
+redbpf = { path = "redbpf/redbpf", features = ["load"] }
 serde = { version = "1.0", features = ["derive"] }
 bincode = "1.3"
 
@@ -20,4 +20,4 @@ xdp-module = { path = "xdp-module" }
 crypto = { tag = "v0.5.0", git = "https://github.com/simplestaking/tezedge" }
 
 [build-dependencies]
-cargo-bpf = { version = "1.2", default-features = false, features = ["build"] }
+cargo-bpf = { path = "redbpf/cargo-bpf", default-features = false, features = ["build"] }
diff --git a/xdp-module/Cargo.toml b/xdp-module/Cargo.toml
index 8532067..17bfb4b 100644
--- a/xdp-module/Cargo.toml
+++ b/xdp-module/Cargo.toml
@@ -10,13 +10,13 @@ path = "src/bin/main.rs"
 required-features = ["probes"]
 
 [dependencies]
-redbpf-macros = { version = "1.2", default-features = false }
-redbpf-probes = { version = "1.2", default-features = false }
+redbpf-macros = { path = "../redbpf/redbpf-macros", default-features = false }
+redbpf-probes = { path = "../redbpf/redbpf-probes", default-features = false }
 bitflags = "1.2"
 serde = { version = "1.0", default-features = false, features = ["derive"] }
 
 [build-dependencies]
-cargo-bpf = { version = "1.2", default-features = false, features = ["bindings"] }
+cargo-bpf = { path = "../redbpf/cargo-bpf", default-features = false, features = ["bindings"] }
 
 [features]
 probes = []
